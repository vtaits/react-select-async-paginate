import { Meta, Story } from '@storybook/addon-docs';

import { SelectFetch } from '../src';
import { argTypes } from './argTypes';

import { CreatableWithNewOptions } from './CreatableWithNewOptions';

<Meta
  title="react-select-fetch/CreatableWithNewOptions"
  component={SelectFetch}
  argTypes={argTypes}
/>

export const Template = (props) => {
  return <CreatableWithNewOptions {...props} />;
}

# Creatable with new options

<Story name="CreatableWithNewOptions">
  {Template.bind({})}
</Story>
