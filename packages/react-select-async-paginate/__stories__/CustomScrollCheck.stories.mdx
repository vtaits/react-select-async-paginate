import { Meta, Story } from '@storybook/addon-docs';

import { AsyncPaginate } from '../src';
import { argTypes } from './argTypes';

import { CustomScrollCheck } from './CustomScrollCheck';

<Meta
  title="react-select-async-paginate/CustomScrollCheck"
  component={AsyncPaginate}
  argTypes={argTypes}
/>

export const Template = (props) => {
  return <CustomScrollCheck {...props} />;
}

# Customization check of the need of load options

<Story name="CustomScrollCheck">
  {Template.bind({})}
</Story>
