import { Meta, Story } from '@storybook/addon-docs';

import { AsyncPaginate } from '../src';
import { argTypes } from './argTypes';

import { RequestByPageNumber } from './RequestByPageNumber';

<Meta
  title="react-select-async-paginate/RequestByPageNumber"
  component={AsyncPaginate}
  argTypes={argTypes}
/>

export const Template = (props) => {
  return <RequestByPageNumber {...props} />;
}

# Request by page number

<Story name="RequestByPageNumber">
  {Template.bind({})}
</Story>
